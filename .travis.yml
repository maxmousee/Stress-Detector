osx_image: xcode8.3
language: objective-c
#cache: cocoapods
#podfile: Example/Podfile
#before_install:
# - gem install cocoapods # Since Travis is not always on latest version
# - pod install --project-directory=iOS
script:
- set -o pipefail && xcodebuild test -project iOS/VSD.xcodeproj -scheme VSD -destination 'platform=Simulator,name=iPhone SE'
