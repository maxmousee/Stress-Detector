# CircleCI 2.0 configuration file
#
version: 2
jobs:
  build:
    docker:
      - image: circleci/openjdk:8-jdk-node-browsers
    parallelism: 4

    working_directory: ~/project

    steps:
      - checkout
        
      # build Linux Lib
      - run:
          name: build C Linux lib
          command: |
            cd Generic/JAVA_C/Linux/; make all